# 水豚噜噜 · 优化与稳定性任务清单（归档）

> 生成 2026-09-14 · 归档 2026-09-16

## 本清单已完成，但其中服务器与排名部分已整体移除

2026-09-16 按用户要求，**云端排名与服务器相关功能已全部删除**，包括：

- 排行榜页签、登录/注册、后台上报线程
- `cloud_rank/`（FastAPI 后端）、`server_rank/`（C++ 后端）
- `rank_client.py` / `rank_ui.py` / `trust_ca.py` / `deploy_tools.py`
- HTTPS 证书与信任逻辑、相关计划任务与部署脚本
- 3 个排名相关测试文件

因此本清单中「二、服务器稳定性任务清单」整节、
以及「一、软件优化」里与排名/HTTPS 相关的条目**均已作废**。

## 仍然有效并保留的成果

| 项 | 状态 |
|---|---|
| 客户端日志轮转（logutil.py，145 MB → 6 MB） | 保留 |
| 健康自检（health.py，已移除 rank 通道） | 保留 |
| 单实例互斥 + 崩溃兜底（guard.py） | 保留 |
| 存储层连接复用（thread-local 长连接） | 保留 |
| 测试与 CI（12 个套件 + GitHub Actions） | 保留 |
| 敏感信息扫描（tools/scan_secrets.py） | 保留 |
| **恢复「历史快照」页** | 新增 |

## 安全收益

移除后不再有任何出站上报（此前每 30 秒上报当日 token 总量给服务器），
本机也不再持有任何服务器凭据。

> 回滚点：tag `before-remove-rank`
